<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top">
  <div class="container-fluid px-lg-4">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <i class="fa-regular fa-compass"></i>
      <span class="brand-text">Wanderlust</span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse gap-3" id="navbarNavAltMarkup">
      <form class="search-shell mx-lg-auto my-3 my-lg-0" role="search" method="GET" action="/listings">
        <div>
          <label>Where</label>
          <input type="search" name="q" value="<%= typeof query !== 'undefined' ? (query.q || '') : '' %>" placeholder="Search destinations" />
        </div>
        <div>
          <label>Guests</label>
          <input type="number" min="1" name="guests" value="<%= typeof query !== 'undefined' ? (query.guests || '') : '' %>" placeholder="Add guests" />
        </div>
        <button class="btn search-btn" type="submit" aria-label="Search">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>

      <div class="navbar-nav ms-auto d-flex align-items-lg-center gap-lg-2">
        <a class="nav-link" href="/listings">Explore</a>
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(currUser) { %>
          <a class="nav-link" href="/bookings">Trips</a>
          <a class="nav-link fw-semibold" href="/logout">Log Out</a>
        <% } else { %>
          <a class="nav-link fw-semibold" href="/signup">Sign Up</a>
          <a class="nav-link fw-semibold" href="/login">Log In</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
